import React from "react";

function Transport({
    onPreprocess,
    onProcPlay,
    onPlay,
    onStop,
    disabled = false,
}){
    return (
        <div>
            Transport
        </div>
    )
}